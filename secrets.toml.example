# 이 파일을 secrets.toml 로 복사한 뒤 실제 값을 채우세요.
# secrets.toml 은 절대 git 에 올리지 마세요 (.gitignore 에 등록됨).

# Google AI Studio에서 발급한 키 (결제 연결된 유료 티어 권장)
GEMINI_API_KEY = "여기에-실제-키-입력"

# 상담사들에게 공유할 접근 코드
ACCESS_CODE = "원하는-접근코드-입력"
